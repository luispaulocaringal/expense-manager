import React, { Component } from 'react';
import { Stats, PlaceInfoContainer, PreferencesTable } from '../components';

export default class Home extends Component {
    render() {
        return (
            <div className="container-fluid bg-white">
                Hello
            </div>
        );
    }
}